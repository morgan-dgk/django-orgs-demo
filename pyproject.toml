[tool.poetry]
name = "mda-client-portal"
version = "0.1.0"
description = ""
authors = ["morgan-dgk <morgan.k@skiff.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
